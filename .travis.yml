sudo: required
dist: trusty
language: node_js
node_js:
  - '6'
before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
install:
  - npm run apply:bitpay
  - cd ionic2 && npm install
script:
  - npm test
  - npm run e2e
# after_success:
#   - npm run exec:coveralls
