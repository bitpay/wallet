'use strict';

var    chai = chai || require('chai');
     should = chai.should();
    bitcore = bitcore || require('bitcore');
   Identity = require('../../js/models/Identity');

describe('Identity model', function() {

  it('should trigger an "updated" event when the profile changes', function() {
  });

  it('can return a copy of the array of wallets', function() {
  });

  it('can create a new wallet and trigger a "newWalletCreated" event', function() {
  });

  it('can join a wallet in creation', function() {
  });

  it('subscribes to new messages received', function() {
  });

  it('reacts when a copayer joins and triggers "newCopayer"', function() {
  });

  it('triggers a "walletCreated" event when the ring is complete', function() {
  });

  it('should fail when created with an invalid extended private key', function() {
  });

  it('should fail when an invalid Profile is provided', function() {
  });

  it('should fail when other invalid parameters are provided', function() {
  });

});

