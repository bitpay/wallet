'use strict';

var  chai = chai || require('chai');
   should = chai.should();
  bitcore = bitcore || require('bitcore');
  Profile = require('../../js/models/Profile');

describe('Profile model', function() {

  it('should trigger an "updated" event when the profile changes', function() {
  });

  it('should fail when invalid parameters are provided', function() {
  });

});

